these tcl files using pftools to do the data manipulation.
However, I didn't keep them since I could not extract the data values fromt he pfb file and do the computationmysef. 
 the pfclm_ET_old file should work, bu the pfclme_ET file contains some muniputation lines which try to load pfb and extract data form pdf file, which failed the execusion.
